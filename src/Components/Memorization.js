import React from "react";
import Contador from "./Contador";

const Memorization = () => {
  return (
    <section>
      <h2>Memorizacion</h2>
      <Contador />
    </section>
  );
};

export default Memorization;
